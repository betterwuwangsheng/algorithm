#include <iostream>
// string 字符串的头文件
#include <string>

using namespace std;

int main() {
    // 定义
    // string s;

    // 输入输出
    // 使用这种方式如果有空格间隔的话会停止
    // 遇到空格会停止
    // cin >> s;

    // 输出
    // cout << s;

    // 整行读入
    // getline(cin, s);
    // cout << s;

    // // 循环读入
    // while (getline(cin, s)) {
    //     cout << s << endl;
    // }

    // while (cin >> s) {
    // }

    // string s, t;
    // cin >> s;

    // 直接赋值
    // t = s;
    // cout << t << endl;

    // t = "stthhh";
    // cout << t << endl;

    // 获取长度
    // int len = s.size();
    // printf("%d\n", len);

    // 遍历 -> 访问
    // for (int i = 0; i < len; ++i) {
    // printf("%c", s[i]);
    // }
    // printf("\n");

    // 关系运算
    // cout << (s <= t) << endl;
    // cout << (s > t) << endl;
    // cout << (s < t) << endl;

    // 连接字符串
    // string res = s + t;
    // cout << res << endl;

    // 插入
    // s.insert(pos -> 起始位置, s2 -> 待插入的字符串);
    // string s;
    // cin >> s;
    // cout << s.insert(1, "sssss");

    // 删除
    // s1.erase(pos -> 起始位置, num -> 删除的数量);
    // string s;
    // cin >> s;
    // cout << s.erase(1, 2);

    // 截取
    // s1.substr(pos -> 起始位置, num -> 截取的数量)
    // string s;
    // cin >> s;
    // string t = s.substr(1, 2);
    // cout << t << endl;

    // 查找
    // s1.find(s2 -> 要查找的字符串, pos -> 从哪个位置开始查找)
    // string s;
    // cin >> s;
    // cout << s.find("string", 0) << endl;

    // 整数转换为字符串
    // string s = to_string(123123);
    // cout << s;

    // 数值转字符串
    // string s;
    // cin >> s;
    // int a = stoi(s);
    // long int b = stol(s);
    // long long c = stoll(s);
    // cout << a << endl << b << endl << c << endl;

    return 0;
}