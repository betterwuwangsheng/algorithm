#include <iostream>

using namespace std;

int n, m, s, t;
int solve(int n, int m, int s, int t) {
    int p = 1;
    while (s != t) {
    }
}
int main() {
    int T;
    scanf("%d", &T);

    while (T--) {
        scanf("%d%d%d%d", &n, &m, &s, &t);

        solve(n, m, s, t);
    }
    return 0;
}