#include <bits/stdc++.h>

using namespace std;

int main() {
    int n, cnt, price;
    scanf("%d", &n);  //需要的铅笔数量

    for (int i = i; i <= 3; ++i) {
        scanf("%d%d", &cnt, &price);
    }
    return 0;
}