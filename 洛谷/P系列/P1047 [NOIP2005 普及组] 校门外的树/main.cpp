#include <algorithm>
#include <iostream>
using namespace std;

int main()
{
  // 马路长度
  // 区域的数目
  int l, number;
  cin >> l >> number;

  int u, v;
  for (int i = 0; i < number; ++i)
    cin >> u >> v;
}