#include <bits/stdc++.h>

using namespace std;

/**
问题描述
伊格内修斯（Ignatius）数学能力很差，他遇到了一个难题，因此他只能求助于埃迪（Eddy）。该问题描述为：f（x）= 5 * x ^ 13 + 13 * x ^ 5 + k * a * x，输入一个非负整数k（k <10000），求出最小非负整数a，使任意整数x，65 | f（x），如果
不存在a，则打印“ no”。



输入值
输入包含几个测试用例。每个测试用例都由一个非负整数k组成，样本输入中有更多详细信息。


输出量
如果找不到a，则输出包含字符串“ no”，否则应输出包含a的行。Sample Output中的更多详细信息。
Output
The output contains a string "no",if you can't find a,or you should output a line contains the a.More details in the Sample Output.


Sample Input
11
100
9999


Sample Output
22
no
43
*/
int main()
{
    int num;
    while(cin >> num){

    }
    return 0;
}
