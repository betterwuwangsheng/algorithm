#include <bits/stdc++.h>

using namespace std;

/**
字母字符串的D对是彼此有距离D的有序字母对。如果字符串的所有D对都不同，则它是D唯一的。如果字符串在每个可能的距离D上都是D唯一的，那么这是令人惊讶的。

考虑字符串ZGBG。它的0对是ZG，GB和BG。由于这三对都不同，因此ZGBG是0唯一的。类似地，ZGBG的1对是ZB和GG，并且由于这两对是不同的，因此ZGBG是1唯一的。最后，ZGBG的唯一2对是ZG，因此ZGBG是2唯一的。因此ZGBG令人惊讶。（请注意，ZG既是0对又是2对ZGBG的事实是不相关的，因为0和2是不同的距离。）

致谢：这个问题的灵感来自《科学美国人》（Scientific American） 2003年12月号的“令人费解的冒险”专栏。

输入值
输入由一个或多个最多为79个大写字母的非空字符串组成，每个字符串本身一行，然后是仅包含一个星号的行，表示输入结束。

输出量
对于每个字母字符串，使用下面显示的确切输出格式输出是否令人惊讶。

Sample Input
ZGBG
X
EE
AAB
AABA
AABB
BCBABCC
*
Sample Output
ZGBG is surprising.
X is surprising.
EE is surprising.
AAB is surprising.
AABA is surprising.
AABB is NOT surprising.
BCBABCC is NOT surprising.
*/
int main() {


    string s;
    int len;

    while(scanf("%d", &s && str!= "*")){

    }
    return 0;
}
