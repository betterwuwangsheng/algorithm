#include <bits/stdc++.h>

using namespace std;

const int MAXN = 10010;
int a[MAXN];
int dp[MAXN];

int main() {
    int k;
    while(cin >> k) {
        for(int i = 0; i < n; ++i)
            cin >> a[i];

    }
    return 0;
}
