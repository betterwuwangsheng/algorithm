#include <bits/stdc++.h>

using namespace std;

int main() {
    int T;
    cin >> T;

    int k = 0;
    while(T--) {
        string s;
        cin >> s;

        int len = s.length();
        //遍历
        for(int i = 0; i < len; i++){
            if(s[i] == '!')
        }
        printf("case #%d:\n%d\n", k++, );
    }
    return 0;
}
