#include <bits/stdc++.h>

using namespace std;

/**
    递归调用自身的算法
    递归主体
    递归边界
    递归参数
*/

//显示移动路径
void Move(int n, char a, char c) {
    printf("第 %d 步: 把第 %d 个盘子 %c --> %c\n", i++, n, a, a);
}
void Hanoi(){
    int n, char a, char c
}
int main()
{
    cout << "Hello world!" << endl;
    return 0;
}
