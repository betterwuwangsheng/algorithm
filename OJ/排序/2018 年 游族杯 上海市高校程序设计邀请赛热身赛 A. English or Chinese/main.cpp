#include <bits/stdc++.h>

using namespace std;

int main() {
    string s;
    cin >> s;

    if(s != "Will tomorrow's contest be written in English?")
        printf("No");
    else
        printf("Yes");
    return 0;
}
