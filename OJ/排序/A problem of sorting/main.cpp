#include <bits/stdc++.h>

using namespace std;

int main() {
    ///×éÊı
    int T;
    scanf("%d", &T);

    int n;
    string s;
    int year;
    for(itn i = 0; i < T; i++) {
        scanf("%d", &n);
        while(n--) {
            cin >> s;
            scanf("%d", &year);
        }
    }

    sort()
    return 0;
}
