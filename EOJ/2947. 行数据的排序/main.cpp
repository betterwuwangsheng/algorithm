#include <bits/stdc++>

using namespace std;

int main()
{
    int T;
    cin >> T;
    while(T--){
        int n;
        cin >> n;
        while(n--){
            int num;
            while(cin >> num){
                if(num != -1){

                }
            }
        }
    }
    return 0;
}
