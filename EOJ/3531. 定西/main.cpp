#include <bits/stdc++.h>

using namespace std;

int main()
{
    int n, k, num;

    ///楼梯一共有 n 层
    ///k -> num 数据的数量
    scanf("%d%d", &n, &k);

    ///腐乳损坏的台阶号
    ///（从楼梯底部到楼梯顶部，台阶序号依次为 1 到 n）。
    ///一次能上一阶、两阶或三阶台阶
    while(k--){
        scanf("%d", &num);
    }
    return 0;
}
