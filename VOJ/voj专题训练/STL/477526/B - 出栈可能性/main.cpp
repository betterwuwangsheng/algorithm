#include <iostream>
#include <stack>

using namespace std;

int n, num;
int main() {
    while (scanf("%d", &n) != EOF) {
        if (n == 0)
            break;
        while (scanf("%d", &num) != EOF) {
        }
    }
    return 0;
}